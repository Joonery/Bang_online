# 규칙 해석 기록

이 문서는 공식 자료에서 구현에 필요한 세부 동작이 명시되지 않았거나 제공 자료 사이에 표현 차이가 있는 부분만 기록합니다. 구현 기준 우선순위는 `docs/BANG_Card_List.pdf` 및 제공 한글 룰북 → 작업 요구사항 → 일반적인 기본판 규칙입니다.

## 캐릭터 배분 방식

- 관련 규칙: 게임 준비 시 인물 카드 배분
- 룰북 확인: 인물 카드를 잘 섞어 각 플레이어에게 한 장씩 나누고 공개한다고 되어 있습니다.
- 모호한 부분: 최초 요구사항에는 "캐릭터 선택 또는 룰북에 맞는 캐릭터 배분"이 모두 허용되어 있으며, 후속 요구사항에서 두 장 중 한 장을 고르는 방식을 명시했습니다.
- 선택한 해석: 후속 요구사항을 우선하여 서버가 16장을 섞은 뒤 각 플레이어에게 서로 겹치지 않는 후보 두 장을 개인적으로 제시하고 한 장을 선택하게 합니다. 다른 플레이어의 후보와 남은 캐릭터 순서는 전송하지 않습니다.
- 코드 위치: `engine/game.js`의 `start()`, `chooseCharacter()`, `public/app.js`의 `openCharacterDialog()`

## 다이너마이트 전달 대상이 이미 다이너마이트를 가진 경우

- 관련 규칙: 폭발하지 않은 다이너마이트의 시계 방향 전달
- 룰북 확인: 다음 플레이어에게 전달한다고 되어 있습니다.
- 모호한 부분: 다음 플레이어가 같은 이름의 카드를 이미 장착한 경우 기본판의 "같은 이름 카드 중복 장착 금지"와 충돌합니다.
- 선택한 해석: 시계 방향으로 다이너마이트가 없는 가장 가까운 생존자에게 넘깁니다. 비공식 룰 설명과 중복 장착 금지를 함께 만족하는 처리입니다.
- 코드 위치: `engine/game.js`의 `nextAliveWithNoDynamite()`, `resolveJudgment()`

## 럭키 듀크의 두 번째 판정 카드 위치

- 관련 규칙: 럭키 듀크는 카드 펼치기 때 두 장 중 하나를 선택
- 룰북 확인: 선택한 한 장의 결과를 적용합니다.
- 모호한 부분: 선택하지 않은 카드의 최종 위치가 제공 자료에 명시적으로 적혀 있지 않습니다.
- 선택한 해석: 두 카드 모두 공개 판정에 사용된 것으로 보고 버린 카드 더미에 둡니다.
- 코드 위치: `engine/game.js`의 `resolveJudgment()`

## 벌쳐 샘과 제거 보상 처리 순서

- 관련 규칙: 제거된 플레이어의 모든 카드 획득, 무법자 제거 보상, 보안관의 부관 제거 벌칙
- 룰북 확인: 각 효과는 제거 즉시 적용됩니다.
- 모호한 부분: 여러 효과가 같은 제거에 발동할 때의 상세 우선순위는 제공 자료에서 한 문장으로 정리되어 있지 않습니다.
- 선택한 해석: 역할 공개 → 벌쳐 샘이 제거자의 카드 획득 → 무법자 현상금 3장 → 보안관의 부관 제거 벌칙 → 승리 판정 순으로 처리합니다. 따라서 보안관이 부관을 제거했다면 같은 제거에서 얻은 카드까지 모두 버립니다.
- 코드 위치: `engine/game.js`의 `eliminate()`

## 휘발성 서버 세션

- 관련 요구사항: reconnect와 영속 저장 여부
- 확인 내용: 브라우저 refresh/일시 단절은 복구해야 하지만 서버 종료 뒤 영속화는 기존 구조와 복잡도를 보고 판단하도록 되어 있습니다.
- 모호한 부분: 없음. 운영 선택을 투명하게 기록합니다.
- 선택한 해석: 192-bit 무작위 token을 브라우저에 저장해 프로세스 생존 동안 신원을 복구합니다. DB는 사용하지 않으며 서버 재시작 시 게임·로그·채팅을 폐기합니다.
- 코드 위치: `server/server.js`, `public/app.js`
